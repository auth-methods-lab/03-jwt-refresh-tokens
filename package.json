{
	"name": "03-jwt-refresh-tokens",
	"version": "1.0.0",
	"description": "Implementación de autenticación stateless con JWT, rotación de refresh tokens en PostgreSQL y blacklist de access tokens en Redis.",
	"main": "src/index.js",
	"scripts": {
		"dev": "node --watch --env-file=.env src/index.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "Israel Luque Sanchez",
	"license": "UNLICENSED",
	"private": true,
	"engines": {
		"node": ">=22.0.0"
	},
	"devEngines": {
		"packageManager": {
			"name": "pnpm",
			"version": "^11.1.1",
			"onFail": "download"
		}
	},
	"type": "module",
	"dependencies": {
		"bcrypt": "6.0.0",
		"cors": "2.8.6",
		"express": "5.2.1",
		"postgres": "3.4.9",
		"redis": "6.1.0",
		"zod": "4.4.3"
	}
}
